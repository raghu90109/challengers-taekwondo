spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/taekwon
    username: postgres
    password: raghu
  flyway:
    enabled: true
    baseline-on-migrate: true
    baseline-version: 1

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true
    open-in-view: false
    properties:
      hibernate:
        format_sql: true
        jdbc:
          time_zone: UTC

  sql:
    init:
      mode: never
server:
  port: 9090